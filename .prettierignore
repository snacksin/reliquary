# Package Managers
package-lock.json
pnpm-lock.yaml
yarn.lock
bun.lock
bun.lockb

# Miscellaneous
/static/

# Stored library content + local backups — user DATA, never code style.
# A --write over these would rewrite Allie's fics; keep prettier out.
/data/
/data.bak-*/
